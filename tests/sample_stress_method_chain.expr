class Counter {
  i32 value;

  bump() -> Counter {
    this.value = this.value + 1;
    return this;
  }

  get() -> i32 {
    return this.value;
  }
}

Counter c = Counter();
c.value = 0;
print c.bump().bump().bump().get();
