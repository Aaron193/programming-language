auto d = {};

auto start = clock();

for (auto i = 0; i < 30000; i++) {
  auto key = "key" + str(i);
  d[key] = i;
}

auto sum = 0;
for (auto i = 0; i < 30000; i++) {
  auto key = "key" + str(i);
  sum += d[key];
}

auto elapsed = clock() - start;

print sum;
print d.size();
print elapsed;
