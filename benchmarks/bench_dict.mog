Dict<str, i32> d = {};

f64 start = clock();

for (i32 i = 0; i < 30000; i++) {
  str key = "key" + str(i);
  d[key] = i;
}

i32 sum = 0;
for (i32 i = 0; i < 30000; i++) {
  str key = "key" + str(i);
  sum += d[key];
}

f64 elapsed = clock() - start;

print(sum);
print(d.size());
print(elapsed);
