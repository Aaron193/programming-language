type Counter struct {
  value i32

  fn bump() Counter {
    this.value = this.value + 1
    return this
  }

  fn get() i32 {
    return this.value
  }
}

var c Counter = Counter()
c.value = 0
print(c.bump().bump().bump().get())
