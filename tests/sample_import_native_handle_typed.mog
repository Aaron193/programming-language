const counter = @import("counter")

const first handle<counter:CounterHandle> = counter.create(10i64)
const second handle<counter:CounterHandle> = counter.create(20i64)

print(counter.read(first))
print(counter.add(second, 5i64))
