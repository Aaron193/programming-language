fn fib(n i32) i32 {
    if (n <= 1)
        return n
    return fib(n - 1) + fib(n - 2)
}

const start f64 = clock()
const result i32 = fib(35)
const elapsed f64 = clock() - start

print(result)
print(elapsed)
