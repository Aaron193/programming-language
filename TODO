- add AST layer in compiler to allow for compile time optimizations and transformations (remove dead code, inline functions, constant expressions, etc.)
- rework import system (@import('')) when it comes to standard library/packages vs user code vs external dependencies (need clear way to distinguish based off import statement)
- compile time decorators using '@' ???
- fix syntax highlight bug when putting large expression on one line instead of multi-lines... weird