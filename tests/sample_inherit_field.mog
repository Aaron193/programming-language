type Base struct {
  x i32
}

type Derived struct < Base {
  y i32

  fn sum() i32 {
    return this.x + this.y
  }
}

var item Derived = Derived()
item.x = 3
item.y = 4

print(item.x)
print(item.y)
print(item.sum())
